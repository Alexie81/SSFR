# SSFR
SSFR — SpectraShard Fourier Router is an experimental routing algorithm for billion-scale vector databases, using Fourier compression, deterministic error bounds, and adaptive shard selection to reduce search latency and resource usage.
